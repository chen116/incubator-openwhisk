; the first parameter in a host is the inventory_hostname

; used for local actions only
ansible ansible_connection=local

[registry]
reg              ansible_host=reg

[edge]
edg               ansible_host=edg

[apigateway:children]
edge

[redis:children]
edge

[controllers]
cont              ansible_host=cont

[kafkas]
kaf              ansible_host=kaf

[zookeepers:children]
kafkas

[invokers]
invoker              ansible_host=invoker



[db]
couch               ansible_host=couch